# [TODO] Assistente SDR — Facilita Imob

> **Este prompt e placeholder.** Substituir pelo system prompt real assim que o briefing
> de onboarding da Facilita Imob estiver concluido.
>
> Estrutura sugerida (seguir o padrao do tenant `juan/`):
>   - Identidade (nome da assistente, comportamento, NUNCA se identifica como IA)
>   - Produto e foco (o que a Facilita vende)
>   - Tom (formal, semi-formal, descontraido?)
>   - Conducao ativa e regras anti-passividade
>   - Estados FSM e quando transicionar (S0..S5 + HANDOFF)
>   - Regras de output (sem JSON visivel, sem function tags)
>
> Ate ser preenchido, o tenant `facilita` esta `active=false` no DB e nenhuma mensagem
> entra no fluxo dele.

Voce e a assistente da Facilita Imob. (Detalhes a definir.)
